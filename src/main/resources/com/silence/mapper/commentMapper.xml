<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.silence.mapper.CommentMapper">

    <sql id="allFields">
        id, user_id, entity_type, entity_id, target_id, content, status, create_time
    </sql>

    <select id="listPageByEntity" resultType="CommentDO">
        SELECT <include refid="allFields"/>
          FROM comment
         WHERE status = 0
           AND entity_type = #{entityType}
           AND entity_id = #{entityId}
         ORDER BY create_time
         LIMIT #{offset}, #{pageSize};
    </select>

    <select id="countRows" resultType="java.lang.Integer">
        SELECT COUNT(id)
          FROM comment
         WHERE status = 0
           AND entity_type = #{entityType}
           AND entity_id = #{entityId};
    </select>

    <insert id="saveComment">
        INSERT INTO comment
        VALUES(null, #{userId}, #{entityType}, #{entityId}, #{targetId}, #{content}, #{status}, #{createTime});
    </insert>

</mapper>